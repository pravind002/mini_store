import 'package:flutter/material.dart';

class Seting extends StatelessWidget {
  const Seting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: const Center(child: Text("Set Up Setting")),
    );
  }
}
